{
  "compilerOptions": {
    "outDir": "./dist/worker",
    "module": "ESNext",
    "target": "ESNext",
    "strict": true,
    "moduleResolution": "node"
  },
  "include": ["src"],  // <- or "src/workers" if worker code is inside a subfolder
  "exclude": ["node_modules"]
}
